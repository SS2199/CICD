name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    env:
      KUBECONFIG: ${{ github.workspace }}\\.kube\\config
      DOCKERHUB_REGISTRY: sindhusambasivam/hello_world

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install Dependencies
        run: npm install

      - name: SonarQube Analysis
        run: |
          curl -o sonar-scanner.zip -L https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-windows.zip
          powershell -Command Expand-Archive -Path sonar-scanner.zip -DestinationPath .
          echo "C:\\Users\\sindhu\\sonar-scanner-5.0.1.3006-windows\\bin\\sonar-scanner.bat"  
        shell: cmd

      - name: Test using Jest
        run: npm test

      #- name: Execute JMeter
       # run: |
        #  cmd /c "C:\\Users\\sindhu\\apache-jmeter-5.6.2\\bin\\jmeter.bat -n -t 'Test Plans\\PetStore-End-to-End-Flow.jmx' -p 'Test Plans\\data\\PetStore_LoadTest.properties' -JTOTAL_THREADS=2 -JTEST_DURATION=60 -l MyRun1.jtl"

      
      - name: Build Docker Image
        run: |
          $dockerImage = "${env:DOCKERHUB_REGISTRY}:${{ github.run_number }}"
          docker build -t $dockerImage .

      - name: Push Docker Image
        run: |
          echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin
          docker push "${env:DOCKERHUB_REGISTRY}:${{ github.run_number }}"

      - name: Deploy to Kubernetes
        run: |
          kubectl apply -f node-web-app-deployment.yaml
          kubectl apply -f node-web-app-service.yaml

      - name: Run Selenium Test
        run: |
          Start-Process -NoNewWindow -FilePath "node" -ArgumentList "helloworld.js"
          Start-Sleep -Seconds 10
          # Install Selenium dependencies and run Selenium tests
          # Replace with the actual commands

      - name: Cleanup
        run: docker logout
